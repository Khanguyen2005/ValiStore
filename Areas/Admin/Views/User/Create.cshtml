@model ValiModern.Models.EF.User
@{
    ViewBag.Title = "Create User";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
<div class="container-xl py-3">
    <h2 class="h5 fw-bold mb-3">Create user</h2>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="row g-3">
            <div class="col-md-6">
                @Html.LabelFor(m => m.email)
                @Html.TextBoxFor(m => m.email, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.email)
            </div>
            <div class="col-md-6">
                @Html.LabelFor(m => m.username)
                @Html.TextBoxFor(m => m.username, new { @class = "form-control" })
            </div>
            <div class="col-md-6">
                @Html.LabelFor(m => m.phone)
                @Html.TextBoxFor(m => m.phone, new { @class = "form-control" })
            </div>
            <div class="col-md-6">
                @Html.LabelFor(m => m.address)
                @Html.TextBoxFor(m => m.address, new { @class = "form-control" })
            </div>
            <div class="col-md-6">
                @Html.LabelFor(m => m.password, "Password")
                @Html.PasswordFor(m => m.password, new { @class = "form-control" })
            </div>
            <div class="col-md-6 d-flex align-items-end">
                <div class="form-check">
                    @Html.CheckBoxFor(m => m.is_admin, new { @class = "form-check-input", id = "is_admin" })
                    <label class="form-check-label" for="is_admin">Admin</label>
                </div>
            </div>
        </div>
        <div class="mt-3">
            <a class="btn btn-outline-secondary" href="@Url.Action("Index")">Cancel</a>
            <button class="btn btn-primary" type="submit">Create</button>
        </div>
    }
</div>