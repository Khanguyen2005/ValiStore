@model ValiModern.Models.EF.Banner
@{
    ViewBag.Title = "Create Banner";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
<div class="container-xl py-3">
    <h2 class="h5 fw-bold mb-3">Create banner</h2>
    @using (Html.BeginForm(null, null, FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.AntiForgeryToken()
        <div class="mb-3">
            @Html.LabelFor(m => m.name)
            @Html.TextBoxFor(m => m.name, new { @class = "form-control", placeholder = "Banner name" })
            @Html.ValidationMessageFor(m => m.name)
        </div>
        <div class="mb-3">
            <label class="form-label">Image</label>
            <div class="img-dropzone border rounded-3 p-4 text-center bg-light position-relative"
                style="cursor:pointer;">
                <input type="file" name="imageFile" accept="image/*"
                    class="position-absolute top-0 start-0 w-100 h-100 opacity-0" />
                <div class="dz-text">
                    <i class="bi bi-cloud-upload fs-1 text-primary"></i>
                    <p class="mb-1">Drag & drop image here or click to choose</p>
                    <small class="text-muted">Allowed: jpg, jpeg, png, gif, webp</small>
                </div>
                <img data-preview src="" alt="preview" class="d-none img-fluid rounded" />
            </div>
            <span class="text-danger">@Html.ValidationMessage("imageFile")</span>
        </div>
        <div>
            <a class="btn btn-outline-secondary" href="@Url.Action("Index")">Cancel</a>
            <button class="btn btn-primary" type="submit">Create</button>
        </div>
    }
</div>
@section scripts {
    <script src="~/Scripts/Admin/image-dropzone.js"></script>
}